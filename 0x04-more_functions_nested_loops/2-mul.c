#include<stdio.h>
#include <ctype.h>
/**
 * mul - Entry point
 * mul - that checks for lowercase character
 * Return program that prints the alphabet in lowercase, using putchar
 * Return: Void
 *  @a: The first number
 *  @b: The second number
 */
int mul(int a, int b)
{
	/* member c - is a char */
	/* parameter int c member c  - is a char */
	return (a * b);
}
