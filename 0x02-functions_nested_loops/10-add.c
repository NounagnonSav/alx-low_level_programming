#include<stdio.h>
/**
 * add - Entry point
 * add - that checks for lowercase character
 * Return program that prints the alphabet in lowercase, using putchar
 * Return: Void
 *  @a: The first number
 *  @b: the second number
 */
int add(int a, int b)
{
	return (a + b);
}

